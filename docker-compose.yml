services:
  architect:
    build: .
    container_name: architect-platform
    ports:
      - "8080:8080"
    environment:
      - SERVER_PORT=8080
      - SPRING_THYMELEAF_CACHE=false
    restart: unless-stopped
